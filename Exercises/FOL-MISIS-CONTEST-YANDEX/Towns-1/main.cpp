#include <iostream>
#include <vector>

using namespace std;

int main() {
    int N(0), tmp1(0), tmp2(0);

    vector<int> A;
    for (int i(0); i < N; ++i) {
            cin >> tmp1 >> tmp2;

    }
    return 0;
}
