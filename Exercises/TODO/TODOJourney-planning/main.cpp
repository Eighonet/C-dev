#include <iostream>
#include <vector>
using  namespace std;

int main() {
    int n(0), tmp(0), checkerGen(0), checkerRCI(0), checkerBSI(0), condRCI(0), condBSI(0);
    for (int i(0); i < n; ++i) {

    }
    return 0;
}
